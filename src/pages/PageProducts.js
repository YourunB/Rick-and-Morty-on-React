import React, { useState, useEffect, useRef } from "react";
import "regenerator-runtime/runtime";
import { PagesLinks } from "../components/PagesLinks";

export const PageProducts = () => {

  return (
    <main className="PageProducts">
        <PagesLinks />
    </main>
  );
};
