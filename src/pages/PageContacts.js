import React, { useState, useEffect, useRef } from "react";
import "regenerator-runtime/runtime";
import { PagesLinks } from "../components/PagesLinks";

export const PageContacts = () => {

  return (
    <main className="PageContacts">
        <PagesLinks />
    </main>
  );
};
